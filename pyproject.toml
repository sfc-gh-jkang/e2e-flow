[project]
name = "e2e-flow"
version = "0.1.0"
description = "E2E Flow with Prefect worker on Snowflake SPCS"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "prefect>=2.14.0",
    "prefect-snowflake>=0.27.0",
]
